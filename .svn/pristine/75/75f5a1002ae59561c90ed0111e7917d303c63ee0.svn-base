#include <iostream>
#include <string>

#include "m1.h"

int main(int argc, char** argv) {

    std::string map_path;
    if(argc == 1) {
        //Use a default map
        map_path = "/cad2/ece297s/public/maps/toronto_canada.streets.bin";
    } else if (argc == 2) {
        //Get the map from the command line
        map_path = argv[1];
    } else {
        //Invalid arguments
        std::cerr << "Usage: " << argv[0] << " [map_file_path]\n";
        std::cerr << "  If no map_file_path is provided a default map is loaded.\n";
        return 1;
    }

    //Load the map and related data structures
    bool load_success = load_map(map_path);
    if(!load_success) {
        std::cerr << "Failed to load map '" << map_path << "'\n";
        return 2;
    }

    std::cout << "Successfully loaded map '" << map_path << "'\n";
    //You can now do something with the map
/*
    std::vector<unsigned> test= find_street_ids_from_name("Sir Francesco Street");
    std::cout<<"um"<<test.size()<<std::endl;
    for (int x = 0; x<test.size();x++)
    {
        std::cout << test[x] <<std::endl;
    }*/
    
    std::vector<unsigned> actual = find_intersection_ids_from_street_names("Barberry Lane", "Old Mill Lane");
    for (int x = 0; x<2;x++)
    {
        std::cout << actual[x] <<std::endl;
    }
    //Clean-up the map related data structures
    std::cout << "Closing map\n";
    close_map(); 
    
    return 0;
}

/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/* 
 * File:   m3_additional.h
 * Author: caiwei7
 *
 * Created on March 18, 2017, 3:42 AM
 */

#ifndef M3_ADDITIONAL_H
#define M3_ADDITIONAL_H
#include "StreetsDatabaseAPI.h"
#include <boost/heap/fibonacci_heap.hpp>
#include <unordered_map>

using namespace std;


struct node {
    unsigned int intersection_id;
    double f = 0;
    double g = 0;
    double h = 0; //cost
    int street_seg_id = -1;
    struct node* parent_node = NULL;
};

struct compare{
    bool operator()( node* const& n1,  node* const &n2){
        return n1 ->f < n2 ->f;
    }
};


double heuristic (unsigned current, unsigned dest);

//vector<unsigned> finish_clean (node* &last, std::unordered_multimap<unsigned, node*> &get_node);
vector<unsigned> finish_clean (node* &last, std::unordered_map<unsigned, node*> &get_node);
// Dijkstra algorism implemented for POI path search. 
std::vector <unsigned> Dijkstra(const unsigned intersect_id_start, 
                           std::unordered_map <unsigned, unsigned> closet_intersection_list, 
                           const double turn_penalty);

#endif /* M3_ADDITIONAL_H */

